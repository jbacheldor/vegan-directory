import './Cards.css'

const RecipeCard: React.FC = () => {
    // think we need to do a situation here where we randomly generate one

    return (
        <div id='recipeCard'>
            <span>title of recipe</span>
            <span>content creator</span>
            <span>meta</span>
        </div>
    )
}

export default RecipeCard;